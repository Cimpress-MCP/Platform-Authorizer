{
  "name": "jwt-custom-authorizer",
  "version": "0.0.1",
  "license": "Apache-2.0",
  "private": true,
  "description": "A custom authorizer for AWS API Gateway for Cimpress Platform services.",
  "main": "index.js",
  "scripts": {
    "test": "./node_modules/.bin/lambda-local --timeout 300 --lambda-path index.js --event-path event.json"
  },
  "author": "Fulfiller Enablement",
  "dependencies": {
    "jsonwebtoken": "^8.1.0",
    "jwks-rsa": "^1.2.1"
  },
  "devDependencies": {
    "aws-sdk": "2.168.0",
    "lambda-local": "1.4.4"
  }
}
